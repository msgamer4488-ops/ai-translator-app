name: ai_language_translator_pro
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # Updated packages to support Android SDK 36 natively
  file_picker: ^11.0.3
  permission_handler: ^14.0.0
  google_mlkit_text_recognition: ^0.16.0
  share_plus: ^13.3.0
  flutter_tts: ^4.2.0
  speech_to_text: ^7.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
